# 使用说明

本页对应 **0.5.0**，介绍宝箱统计、探索辅助、键盘与 Xbox 手柄操作。安装和文件冲突处理见 [安装指南](INSTALLATION.md)，完整传送与返程流程见 [全传送指南](TRAVEL.md)，项目概览见 [首页](../README.md)。

## 目录

- [按用途选择功能](#按用途选择功能)
- [宝箱标记与统计](#宝箱标记与统计)
- [宝箱快捷键](#宝箱快捷键)
- [探索辅助](#探索辅助)
- [全传送入口](#全传送入口)
- [手柄与输入切换](#手柄与输入切换)
- [清单位置与面板显示](#清单位置与面板显示)
- [常见问题](#常见问题)
- [范围与问题反馈](#范围与问题反馈)

## 按用途选择功能

| 想做什么 | 使用功能 | 作用 |
| --- | --- | --- |
| 在地图上找遗漏宝箱 | 宝箱标记 | 使用原生地图箱标，按当前显示模式区分已开／未开 |
| 判断哪些地图没有收集完 | 各地图宝箱收集 | 显示完整“大地图 / 地点”路径、两组计数和剩余未开数量，可筛选遗漏 |
| 看清当前地图的未探索区域 | 地图全显 | 临时显示已加载／选中地图的未探索分块 |
| 在游戏原生菜单中显示未到访地点 | 未到访传送点 | 按当前地区的原生菜单许可临时补显，保留原生禁用条件 |
| 跨地区或返回流程中已无法进入的地图 | 全传送清单 | 不以剧情或到访记录限制清单内地点，并记录首次出发点供精确返程 |

“未到访传送点”与“全传送清单”是两个独立功能。前者调整游戏自己的传送菜单，后者使用 Mod 的独立清单；开关前者不会改变后者的范围。

## 宝箱标记与统计

顶部“显示模式”每次启动默认为**本周目**。使用“切换模式”可以改为**继承记录（多周目）**：

- **闭合箱标：未开。**当前显示模式下尚未开启。
- **开启箱标：已开。**当前显示模式下已经开启。

标记包含对应区域中尚未发现的宝箱。“继承记录”模式把当前存档继承的记录与本周目新开的宝箱一同计入；因此，以前周目已开、本周目未开的箱子，在两种模式下可以显示不同图标。

| 面板文字 | 含义 |
| --- | --- |
| 本周目已开 | 当前存档本周目打开的宝箱 |
| 继承记录已开 | 当前存档继承的记录，并计入本周目新开的宝箱 |
| 本周目当前地区已开 | 当前地区的本周目计数 |
| 继承记录当前地区已开 | 当前地区的继承记录计数，包含本周目新开的宝箱 |

切换显示模式只改变箱标、剩余未开数量和遗漏筛选的依据，两组统计始终独立显示。打开区域地图后可查看当前地区计数。
继承记录只来自**当前读取的存档**，不会合并机器上的其他存档；读取旧档后，统计随该存档回退。

收集清单包含 **59 个地图分组、566 个正式宝箱**，使用“大地图 / 地点”完整名称定位。当前地区统计可能包含相邻道路，而清单将道路分别列出：例如卢安外围地区共 26 箱，阿伊纳街道单独统计 3 箱。迷宫计数包含各楼层，同名古罗尼山道区分柏斯／卢安。

没有宝箱的地图不列入收集清单，尚未到达的地点也会显示；有标记或统计不代表正常剧情已能进入。宝箱标记不替玩家开箱，不直接修改物品或宝箱记录，成就仍由游戏判定。

## 宝箱快捷键

| 功能 | 键盘 | Xbox 手柄 |
| --- | --- | --- |
| 切换模式：本周目／继承记录 | F6 | View + X |
| 显示/隐藏面板，地图标记继续生效 | F7 | View + B |
| 打开／收起各地图宝箱收集清单 | F8 | View + A |
| 暂停／恢复宝箱地图标记 | F9 | View + RS |
| 清单切换全部／仅看遗漏 | F10 | View + Y |
| 清单上一页 | PgUp | View + LB |
| 清单下一页 | PgDn | View + RB |

暂停时宝箱恢复原版地图显示，面板会提示“宝箱标记已暂停（原版显示）”。显示/隐藏只控制面板，暂停／恢复只控制宝箱标记，两者都不会关闭探索辅助。
筛选和翻页仅在对应清单可见时生效；F8／View + A 也可以恢复隐藏的面板并打开宝箱清单。

## 探索辅助

| 功能 | 键盘 | Xbox 手柄 |
| --- | --- | --- |
| 地图全显开／关 | Ctrl + F6 | View + 十字键 ↑ |
| 未到访传送点开／关 | Ctrl + F8 | View + 十字键 ↓ |

两个开关均默认关闭，重启后关闭，F9／View + RS 不影响它们。面板将名称与实际状态一起显示，例如“地图全显：已关闭”；遇到等待应用时，先等待地图回到浏览状态。

**地图全显**只影响原生当前加载或选中地图的未探索分块，保留楼层选择、淡入淡出和脚本禁用限制。它不会补充剧情尚未提供的地图目录，也不保证所有隐藏图标都出现。关闭后，未实际探索的分块恢复隐藏。

**未到访传送点**自动枚举当前地区的原生传送记录：目的地已被游戏登记、最终未被禁用，且所属分组未被禁用时，可以在尚未到访的情况下临时显示，必要时同时显示其地点分组。它没有固定点位数量，不要求先到访城镇或道路锚点；原生静态条件、内部点和特殊标识过滤仍然保留。

此开关采用的是**原生菜单许可**，不等于完整剧情可达判断，显示的地点仍可能越过入口剧情、物理障碍或迷宫层级。确认窗口和传送前后脚本继续由游戏执行。全传送清单另行提供不受这些目的地剧情条件限制的入口，见 [全传送指南](TRAVEL.md)。

开关在区域地图、全地点列表及城镇／道路子列表的浏览阶段原地生效，无需关图重开。确认弹窗或转场期间会等待返回浏览阶段后应用。刷新保留地图镜头和缩放；临时选中项消失时，游戏重新选择有效地点，列表滚动位置可能重置。切换后当前子列表或整个原生传送清单合法为空时，地图会正常关闭，功能仍可继续使用。

Mod 不把这些临时显示批量写成探索或到访记录；实际行走、传送和自动保存仍可能记录真实进度。关闭辅助不会撤销已经完成的探索。详细回归步骤见仓库中的 [探索辅助测试指南](https://github.com/blockshy/sky2-chest-tracker/blob/main/docs/EXPLORATION_TESTING.md)。

## 全传送入口

在自由行动时打开游戏**区域地图**，等待动画结束，再按 `Ctrl + F10` 或 `View + 十字键 ←` 打开全传送清单。它提供 155 个原生地点、迷途之森补箱入口和精确返程，共 157 项、16 页。

选择目的地后需两次独立确认；连续传送保留最初的出发位置与朝向。使用前保留独立存档，切档前先返程或退出重启；返程记录不绑定存档栏位，原生剧情和自动保存仍会运行。

完整键位、前往／返回步骤、特殊地点位置及重启后的历史选择见 [全传送指南](TRAVEL.md)。

## 手柄与输入切换

**View 是双窗口键；RS 是按下右摇杆，不是拨动摇杆。**LB／RB 是肩键，LT／RT 是扳机。

先按住 View，再按功能键。可以保持 View 按住并反复点按 LB／RB 翻页；传送清单用 LT／RT 逐项选择。每次操作只认一次新的按下，长按不连续移动，扳机需释放后再按。

单独短按 View 保留游戏原有用途，改为松开时触发一次。组合期间暂时屏蔽传给游戏的手柄输入；先松开 View 时，仍按住的功能键也不会突然传给游戏。**不要使用旧版 View + Menu**，该组合可能唤起系统 Xbox 窗口；宝箱暂停使用 View + RS。

键盘与手柄可以同时使用，连接手柄不会禁用键盘。提示随实际键鼠或手柄操作热切换，保持完整组合键显示。重连手柄或切回游戏时，先松开所有控制，再重新使用组合。

当前提示采用 Xbox ABXY 布局。自定义 Steam Input 布局需保留 View 的手柄输出；将 View 改成键盘按键的布局不适用。

## 清单位置与面板显示

宝箱页码与传送选中项分别保留。关闭清单、隐藏面板或切换到另一份清单后，重新打开恢复各自位置；传送选择恢复不代表确认恢复，仍需重新两次确认。

宝箱数据暂时不可用时保留页码，恢复有效数据后按实际页数校正。条目减少或布局变化使页码越界时退至最后有效页；切换宝箱显示模式或遗漏筛选仍回第一页。这些位置只保存在本次进程内，重启后清除，不写文件或存档，也不保证读档后必然清除。

面板以 1920×1080 为基准等比缩放：1440p 约为 1.33 倍、4K 为 2 倍；字体、间距与清单同步缩放，分辨率切换即时生效。超宽屏按高度缩放，较窄屏幕受宽度限制；长地图路径可换行。输入设备切换后，提示和面板宽度随内容调整。

## 常见问题

- **没有面板：**先按 F7／View + B 检查是否只是隐藏。仍未出现时，按 [安装指南](INSTALLATION.md) 核对完整安装包、游戏版本、DLL 位置和日志；源码压缩包不能直接安装。
- **继承图标与本周目不同：**这是预期行为。以前周目已开的宝箱，本周目可能尚未开启；继承统计不会合并其他存档。
- **地区计数与清单中的道路计数不同：**地区可能合并相邻道路，清单逐条列出道路，迷宫则包含各楼层。
- **宝箱图标恢复成原版：**检查是否显示“宝箱标记已暂停（原版显示）”，按 F9／View + RS 恢复。
- **手柄组合不响应：**切回游戏并松开全部控制，再先按 View 后按功能键；RS 要按下，肩键与扳机用途不同，并检查 Steam Input 是否保留 View 输出。
- **开启未到访传送点后没有新增地点：**当前地区可能没有符合原生许可的未到访点，或仍在等待确认／转场结束；这不等于全传送清单不可用。
- **开关传送辅助后地图自动关闭：**合法空列表会正常关图，重新打开即可继续操作。若面板同时显示“不可用”，请保留操作时间附近的日志，说明当时的列表层级。
- **全传送无法确认或重启后需要返程：**先打开稳定的区域地图，按面板具体原因处理；历史返程须自行核对地点与时间，详见 [全传送常见问题](TRAVEL.md#常见问题)。
- **同名 DLL 冲突、手动／脚本混用或执行策略阻止脚本：**按 [安装指南](INSTALLATION.md) 核对文件归属及手动操作方式。不要覆盖未知 DLL、篡改归属记录，或为了运行脚本修改不允许更改的设备策略。

## 范围与问题反馈

宝箱状态、读档回退、地区统计、清单、Xbox 组合键、热切换和重连已有实机验证；地图全显、未到访传送点及地图内即时刷新已有用户本机正常反馈。全传送已有一周目末期、二周目前期相关地点及往返反馈，不能代表全部地点和所有章节组合均已逐一验证。

尚未逐箱走访全部 566 个宝箱；其他手柄类型、自定义布局、DXVK、第三方注入工具和所有剧情组合未全面覆盖。验证范围见仓库中的 [测试说明](https://github.com/blockshy/sky2-chest-tracker/blob/main/docs/TESTING.md)。

反馈问题时提供 Mod 版本、游戏构建、地图／章节、输入设备、操作步骤及完整提示。日志位于游戏目录的 `Sky2ChestTracker/tracker.log`；附相关片段前移除个人信息，无需公开整份存档或内存快照。
